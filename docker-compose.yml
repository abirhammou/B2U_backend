version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: b2u-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: B2U_hub

volumes:
  mongo_data:
